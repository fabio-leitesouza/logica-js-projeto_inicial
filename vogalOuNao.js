//crie uma função com um parametro em que o usuário digita uma letra e o sistema retorna um alert se ele é uma vogal ou não.

function isVogal(letra){
    if(letra == 'a' || letra == 'e' || letra == 'i' || letra == 'o' || letra == 'u')
}